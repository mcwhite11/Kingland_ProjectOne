@{
    Session.Remove("user");
    Session.Remove("isAdmin");
    Session.Remove("isHR");
    Session.Remove("isIT");
    Session.Remove("isPR");
    Session.Remove("isHM");
    Session.Abandon();
    Response.Redirect("../Default.cshtml");
}

