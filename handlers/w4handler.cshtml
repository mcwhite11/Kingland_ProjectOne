@{
    //Set username
    var user = Session["user"];

	var db = Database.Open("kinglandDatabase");
    
    //Update the table.
    db.QuerySingle("UPDATE users SET w4Done = '1' WHERE user = @0", user);
	
	//Move on to the next form
	Response.Redirect("../start.cshtml?form=8");
}
