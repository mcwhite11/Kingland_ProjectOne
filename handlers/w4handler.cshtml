@{
    //Set username
    var user = Session["user"];

	var db = Database.Open["kingland"];
    
    //Update the table.
    db.singleQuery("UPDATE users SET w4Done = '1' WHERE user = '@0'", user);
	
	//Move on to the next form
	Response.Redirect("../start.cshtml?form=8");
}
